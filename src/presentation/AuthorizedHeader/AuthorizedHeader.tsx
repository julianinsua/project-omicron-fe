import { FC } from 'react'
import styles from './authorizedHeader.module.scss'

const AuthorizedHeader: FC<PropTypes> = () => {
  return <div className={styles.container}>pepito</div>
}

interface PropTypes {}

export default AuthorizedHeader
